declare type LoadedFile = [filePath: string, content: string];

export declare function loadRelatedSchemaFiles(filesPath: string): Promise<LoadedFile[]>;

/**
 * Given folder name, returns list of all files composing a single prisma schema
 * @param folderPath
 */
export declare function loadSchemaFiles(folderPath: string): Promise<LoadedFile[]>;

export { }
